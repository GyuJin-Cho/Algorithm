#include <string>
#include <vector>
#include <iostream>
using namespace std;

vector<int> solution(int brown, int yellow)
{
    vector<int> answer;

    int sum = brown + yellow;

    for(int )

    return answer;
}

int main()
{
    vector<int> v = solution(10, 2);

    for (auto i : v)
        cout << i;

    return 0;
}